
const forgotInitialState = {

}

const forgotReducer = (state = forgotInitialState, action: any) => {

    switch (action.type) {

        default: {
            return state
        }
    }
}

export default forgotReducer;