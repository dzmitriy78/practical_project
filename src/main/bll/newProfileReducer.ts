
const newProfileInitialState = {

}

const newProfileReducer = (state = newProfileInitialState, action: any) => {

    switch (action.type) {

        default: {
            return state
        }
    }
}
export default newProfileReducer;