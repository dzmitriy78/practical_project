import React from 'react';
import Header from "./Header";
import Routing from "./Routing";

const Main = () => {
    return (
        <div>
            <Header/>
            <Routing/>
        </div>
    );
};

export default Main;